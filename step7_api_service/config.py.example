#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""
API 服务配置（示例模板）
- 复制为 config.py 并填入真实密码
- 云端部署时使用 DATABASE_URL 环境变量，无需此文件
"""
import os

# 数据库配置（本地开发用，云端用 DATABASE_URL 环境变量替代）
DB_CONFIG = {
    "host": "127.0.0.1",
    "port": 3306,
    "user": "weibo_api_reader",
    "password": "YOUR_PASSWORD_HERE",
    "database": "weibo_comments",
    "charset": "utf8mb4",
}

# 服务配置
SERVICE_HOST = "127.0.0.1"
SERVICE_PORT = 8000

# 缓存配置（秒）
CACHE_TTL = 300

# 情感分析采样上限
SENTIMENT_MAX_SAMPLE = 5000
